spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto:
  datasource:
    url: jdbc:mysql://localhost:3306/online_store
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    url: jdbc:mysql://localhost:3306/online_store
    user: root
    password:
  servlet:
    multipart:
      max-file-size: 9000000

logging:
  level:
    org.springframework: ERROR
    org.academy: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: C:/Users/админ/Downloads/OnlineStoreDemo/OnlineStoreDemo/logs/app.log

server:
  error:
    whitelabel:
      enabled: false

    path: /error